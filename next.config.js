/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'sneakernews.com',
            'images.unsplash.com',
        ]
    }
}

module.exports = nextConfig
